# Install as /etc/harvey-pw-fleet/database.env with owner root and mode 0600.
# Use the same database name, user, and password encoded in DATABASE_URL in app.env.
POSTGRES_DB=harvey_pw_fleet
POSTGRES_USER=pwfleet_app
POSTGRES_PASSWORD=
